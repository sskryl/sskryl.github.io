# Зависимости serverless-функции веб-API на Vercel (api/index.py).
# Только то, что точно ставится из колёс (без нативных сборок).
flask==3.0.3
python-dotenv==1.0.1
# LLM-сомелье (эндпоинт /api/sommelier). Чистый Python-пакет, без нативных сборок.
openai>=1.40
