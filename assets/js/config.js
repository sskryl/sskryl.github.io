// ============================================================================
//  Конфигурация кинотеатра
// ============================================================================
//  Сайт работает БЕЗ ключей "из коробки" на локальном каталоге public-domain
//  фильмов (data/catalog.json) — их можно смотреть прямо на сайте.
//
//  Если вы хотите подключить огромную базу фильмов (постеры, описания,
//  рейтинги, трейлеры) — получите бесплатный API-ключ на
//  https://www.themoviedb.org/settings/api и вставьте его ниже.
//
//  ВНИМАНИЕ: на статичном сайте ключ виден в коде. Используйте бесплатный
//  read-ключ TMDB — он предназначен для публичного чтения и не даёт доступа
//  к аккаунту. Не вставляйте сюда приватные/платные ключи.
// ============================================================================

window.CINEMA_CONFIG = {
  // Название кинотеатра (выводится в шапке и заголовке вкладки)
  siteName: "Kinoflex",

  // Ключ TMDB API (v3). Оставьте пустым, чтобы работать только на локальном
  // каталоге public-domain фильмов.
  tmdbApiKey: "e7f47dd13caf23736103012d98dff159",

  // Язык запросов к TMDB
  tmdbLanguage: "ru-RU",

  // Ссылка на Telegram-бота с психотестом (показывается баннером на сайте)
  telegramBotUrl: "https://t.me/movie_o_bot",

  // --- Phase 2: вход через Telegram + синхронизация с ботом ---
  // Имя бота БЕЗ @ (для кнопки «Войти через Telegram»).
  telegramBotName: "movie_o_bot",

  // Адрес развёрнутого веб-API (bot/web_api.py). Пусто = вход выключен,
  // сайт работает только на локальном профиле (localStorage).
  apiBaseUrl: "https://kinoflex-iota.vercel.app",

  // --- Реклама и баннеры ---
  // Сайт готов к монетизации. Включите ads.enabled и задайте либо AdSense,
  // либо свои баннеры. Слоты помечаются «Реклама» и стоят ненавязчиво
  // (между рядами, над каталогом, в карточке фильма).
  ads: {
    enabled: true,             // главный выключатель рекламы
    label: "Реклама",          // подпись над блоком
    adsenseClient: "",         // Google AdSense, напр. "ca-pub-1234567890123456"
    adsenseSlots: {            // id рекламных блоков AdSense по местам
      home: "",
      catalog: "",
      detail: "",
    },
    // Свои баннеры (показываются, если AdSense не задан). Можно несколько —
    // выбирается случайный. { image, link, alt }
    banners: [
      // { image: "https://.../banner.jpg", link: "https://partner.example", alt: "Промо" }
    ],
    showPlaceholder: true,     // показывать заглушку, если ничего не настроено
  },
};
